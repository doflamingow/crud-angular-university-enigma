import { CustomAgePipe } from './custom-age.pipe';

describe('CustomCurrencyRpPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomAgePipe();
    expect(pipe).toBeTruthy();
  });
});
